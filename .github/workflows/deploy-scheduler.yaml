name: Deploy Scheduler

on:
  release:
    types: [published]

jobs:
  deploy:
    uses: ./github/actions/deploy-to-ecs.yaml
    with:
      service: scheduler
      version: ${{ github.event.release.tag_name }}